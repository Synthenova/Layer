# Project Direction

- The basic static HTML storefront is archived. Do not continue feature work in the root `index.html` storefront.
- From now onward, build and modify the Shopify theme only. Active storefront work belongs under `theme/`.
